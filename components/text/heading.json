{
  "collectionName": "components_text_headings",
  "info": {
    "name": "Heading",
    "icon": "heading",
    "description": ""
  },
  "options": {},
  "attributes": {
    "text": {
      "type": "string",
      "maxLength": 350,
      "required": true,
      "minLength": 2
    },
    "level": {
      "type": "enumeration",
      "enum": [
        "one",
        "two",
        "three",
        "four",
        "five",
        "six"
      ],
      "default": "three",
      "required": true
    },
    "tilt": {
      "type": "enumeration",
      "enum": [
        "down",
        "even",
        "up"
      ],
      "default": "even",
      "required": true
    },
    "justify": {
      "type": "enumeration",
      "enum": [
        "left",
        "center",
        "right"
      ],
      "default": "left"
    },
    "align": {
      "type": "enumeration",
      "enum": [
        "top",
        "center",
        "bottom"
      ],
      "default": "center"
    }
  }
}
