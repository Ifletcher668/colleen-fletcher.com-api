{
    "collectionName": "components_media_single_images",
    "info": {
        "name": "Single Image",
        "icon": "camera"
    },
    "options": {},
    "attributes": {
        "file": {
            "model": "file",
            "via": "related",
            "allowedTypes": ["images"],
            "plugin": "upload",
            "required": false
        },
        "is_circle": {
            "type": "boolean",
            "default": false,
            "required": false
        },
        "has_border": {
            "type": "boolean",
            "default": true,
            "required": false
        },
        "justify": {
            "type": "enumeration",
            "enum": ["left", "center", "right"],
            "default": "left"
        },
        "align": {
            "type": "enumeration",
            "enum": ["top", "center", "bottom"],
            "default": "center"
        }
    }
}
